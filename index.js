/**
 * @file JSDoc や型チェックのエラーが表示されるか検証するためのファイル.
 */

/**
 * Example.
 *
 * @param {string} str - String.
 * @param {number} num - Number.
 * @example Example
 */
function exampleFunction(str, num) {
  const result = str / 2;
  console.log(result);
}

/**
 * @type {string} Sring.
 */
const str = 1;

/**
 * Example.
 * 
 * @example Example
 */
function example() {
  return 1;
}
